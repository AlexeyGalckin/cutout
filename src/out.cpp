#include "out.h"
//
#include <fstream>
#include <iterator>
#include <cstddef>
//
namespace cut
{
	void out::operator()(const opt& o)
	{
		//std::ifstream is;
		////
		//is.open(f.data(), std::ios::in | std::ios::binary);
		////
		//is.seekg(r.bno() * s + r.off());
		////
		//while (s > 0)
		//{

		//}
	}
}