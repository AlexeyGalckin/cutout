#include "fss.h"
//
#include <fstream>
#include <iterator>
#include <cstddef>
//
namespace cut
{
	fss::fss(const std::string_view s)
	{
		//std::ifstream is;
////
//is.open(f.data(), std::ios::in | std::ios::binary);
////
//is.seekg(r.bno() * s + r.off());
////
//while (s > 0)
//{

//}
	}
}